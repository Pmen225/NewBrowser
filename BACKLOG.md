# Backlog

## High Priority

- Add structured citation objects from the sidecar so the UI can render trustworthy source labels, timestamps, and per-result provenance instead of URL extraction heuristics.
- Add a real browser-shell product surface if native browser chrome ownership becomes available. The current sidecar shell is a visual mock, not a browser replacement.

## Medium Priority

- Add richer visual smoke coverage for the extension panel in a real browser environment.
- Add explicit toast feedback for saved prompt mutations and provider lock/unlock actions.
- Add per-prompt naming/editing instead of title auto-generation only.

## Low Priority

- Add optional extension `options_ui` if settings outgrow the in-panel surface.
- Add screenshot-based regression coverage once the repo adopts a supported browser test dependency.
