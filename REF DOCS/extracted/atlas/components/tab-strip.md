# Atlas tab strip

From binary strings (Aura): no explicit tabHeight/stripHeight strings found. CGSize/CGRect and view layout in binary.

See atlas/tokens/binary-strings-ui-candidates.txt for UI-related strings. If Assets.car or design-system bundle contains layout constants, see atlas/assets/.

**Status:** unknown — not found in source (binary strings only).
